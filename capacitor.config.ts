import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'fr.oxypomme.mescomptes',
  appName: 'Mes Comptes',
  webDir: 'dist',
  bundledWebRuntime: false,
}

export default config
